#include <stdio.h>

/**
* main - Prints its name
* @argc: Arg count
* @argv: Args
* Return: 0 on success
*/
int main(__attribute__((unused)) inr argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
